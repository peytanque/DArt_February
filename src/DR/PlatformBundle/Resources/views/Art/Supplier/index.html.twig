{% extends 'base.html.twig' %}

{% block body %}
    <h1>Supplier list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for supplier in suppliers %}
            <tr>
                <td><a href="{{ path('dr_supplier_show', { 'id': supplier.id }) }}">{{ supplier.id }}</a></td>
                <td>{{ supplier.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('dr_supplier_show', { 'id': supplier.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('dr_supplier_edit', { 'id': supplier.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dr_supplier_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
