{% extends 'base.html.twig' %}

{% block body %}
    <h1>Support</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ support.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ support.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dr_support_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dr_support_edit', { 'id': support.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
